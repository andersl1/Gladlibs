using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using SharpNL;

public class valuesFromInput : MonoBehaviour
{
    public static List<string> inputFromUser = new List<string> {"That", "was", "super"};

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
